Makes using Pulumi EZ

# How to install:

- install latest release
- move build to /usr/local/bin

# How to use:

- Make sure you have Pulumi cli installed
- `cd` into directory of the Pulumi project
- then run `ez-pulumi`
